
# The Problem

Given a singly-linked list, that may contain loop, find first element of that loop.

# Example


Given the list:

```
           F <- E
           /    ^
          v     /
A -> B -> C -> D
```

The result is ``C``.
